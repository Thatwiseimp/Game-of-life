function makearray(x,y){
  let array = new Array(x);
  for (let i = 0; i < x; ++){         
     }
}

function setup() {
  createCanvas(400,400);
}

function draw() {
  background(0,0,0);
  ellipse(100, 200, 50, 50);
}